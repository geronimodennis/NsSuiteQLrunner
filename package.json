{
  "name": "netsuite-suiteql-runner",
  "version": "1.1.2",
  "description": "SDF SuiteApp for running SuiteQL from a NetSuite SPA.",
  "scripts": {
    "clean": "gulp clean",
    "build": "gulp build",
    "bundle": "gulp bundle",
    "validate": "npm run bundle && suitecloud project:validate",
    "deploy": "npm run bundle && suitecloud project:deploy"
  },
  "devDependencies": {
    "@hitc/netsuite-types": "^2024.2.2",
    "@oracle/netsuite-uif-types": "^9.0.0",
    "@oracle/suitecloud-cli": "^3.1.2",
    "@rollup/plugin-terser": "^0.4.4",
    "gulp": "^4.0.2",
    "gulp-typescript": "^6.0.0-alpha.1",
    "rollup": "^4.18.0",
    "typescript": "^5.5.4"
  }
}
